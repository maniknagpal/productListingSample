import React, {useState} from 'react';
import {Text, View} from 'react-native';
import FairDeal from './src/screens/FairDeal';

const App = () => {
  return (
    <View style={{flex: 1}}>
      <FairDeal />
    </View>
  );
};

export default App;
